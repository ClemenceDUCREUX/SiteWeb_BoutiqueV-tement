{
  "name": "loue-le-look",
  "version": "1.1.0",
  "private": true,
  "description": "Projet web (frontend statique + backend Express) — boutique de vêtements.",
  "workspaces": [
    "backend"
  ],
  "scripts": {
    "dev": "npm --workspace backend run dev",
    "start": "npm --workspace backend start",
    "format": "prettier --write \"**/*.{html,css,js,md,json}\"",
    "lint": "prettier --check \"**/*.{html,css,js,md,json}\""
  },
  "devDependencies": {
    "prettier": "^3.3.3"
  }
}
